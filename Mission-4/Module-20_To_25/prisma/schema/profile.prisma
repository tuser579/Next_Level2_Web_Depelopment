model Profile {
    id String @id @default(uuid())
    profilePhoto String?
    bio String?

    userId String @unique
    user User @relation(fields: [userId], references: [id])

    @@map("profiles")
}